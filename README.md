# Domin Technical Submission

* **Part 1:** System diagram is located in `docs/` (includes the PDF and the source `.drawio` file).
* **Parts 2 & 3:** Implementation source code is located in `src/` as `sensorInputControl.c` and `powerRailControl.c` respectively.